TTS>=0.22.0
# Pulls in av (PyAV) >=11. Its prebuilt wheel bundles its own FFmpeg, which
# duplicate-loads against the system FFmpeg torchcodec (below) links to —
# see README for the from-source install that avoids it.
faster-whisper>=1.0.0
sounddevice>=0.4.6
pynput>=1.7.6
# Reads the USB mic's mute button, which drives the kiosk. Optional at runtime —
# joulie.handset degrades to keyboard/UI control if it or the mic is missing.
hidapi>=0.14
requests>=2.31
# Memory/swap pressure sampling in joulie/power.py.
psutil>=5.9
chromadb>=0.5.0
sentence-transformers>=3.0.0
transformers>=4.41,<4.46
torchcodec>=0.14.0
pdfplumber>=0.11.0
gradio>=4.44.0,<5.0.0
# Gradio 4.44 uses the pre-1.0 Starlette TemplateResponse(name, ctx) calling
# convention; Starlette 1.x moved to TemplateResponse(request, name, ...) and
# crashes with "unhashable type: dict" when Gradio serves index.html.
starlette>=0.38,<0.42
fastapi>=0.110,<0.116

# dev
pytest>=8.0
